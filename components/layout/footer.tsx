
   export default function Footer() {
   return (      <footer className="mx-auto max-w-7xl px-6 pb-10 text-white/50 text-sm">
        © {new Date().getFullYear()} voidcore technologies — Minimal builds, maximal outcomes.
      </footer>)}